namespace Domain;

/// <summary>
/// Anchors this assembly for reflection-based tests and DI registration, so no
/// caller has to hard-code an assembly name string.
/// </summary>
public static class AssemblyMarker;
