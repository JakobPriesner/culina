.svelte-kit
build
node_modules
pnpm-lock.yaml
# Generated from the backend's OpenAPI document; never hand-edited, so never
# reformatted either.
src/lib/api/generated

# Compiled from messages/*.json by the Paraglide Vite plugin.
src/lib/paraglide

# Paraglide owns this directory and writes a cache, a README and a .meta.json
# into it, none of it formatted and none of it tracked — the project's own
# .gitignore keeps everything here but the settings. Only the settings are ours.
project.inlang/*
!project.inlang/settings.json
